# Day 6 Notes

## What I learned today
- More on classes of functions: constant O(1), logarithmic O(log n), 
  linear O(n), quadratic O(n²), exponential O(2^n) and how they rank in efficiency
- Comparing classes of functions: why an O(n) algorithm will 
  always eventually outperform O(n²) regardless of constants, once n is large enough
- Python lists: creating, accessing, slicing, appending, removing, sorting
- Looping through lists with for and enumerate()

## What I built today
- No projects today. Just practiced code problems to familiarize myself with list operations and methods

## One thing that confused me
- Why tuples exist when lists can do everything they do. The immutability aspect makes sense in theory but I haven't felt the practical need for it yet. Expecting it to click once I use it in a real project

## What I'll do tomorrow
- Python dictionaries, tuples and sets in depth
- Big O, Omega, Theta concepts
- Build the Contact Book project